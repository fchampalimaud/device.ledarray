<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Harp.LedArray.Design.Controls"
             xmlns:conv="clr-namespace:Harp.LedArray.Design.Converters"
             xmlns:local="clr-namespace:Harp.LedArray;assembly=Harp.LedArray"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:vm="clr-namespace:Harp.LedArray.Design.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Harp.LedArray.Design.Views.LedArrayView"
             x:DataType="vm:LedArrayViewModel">

  <UserControl.Resources>
    <conv:EnableFlagConverter x:Key="EnableFlagConverter"/>
    <conv:PayloadFieldConverter x:Key="PayloadFieldConverter"/>
    <conv:VisualStatusColorConverter x:Key="VisualStatusColorConverter"/>
  </UserControl.Resources>         

  <Design.DataContext>
    <!-- This only sets the DataContext for the previewer in an IDE,
         to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
    <vm:LedArrayViewModel />
  </Design.DataContext>

  <DockPanel>
    <Grid Margin="10" DockPanel.Dock="Top"
          ColumnDefinitions="Auto, Auto, *, Auto, Auto"
          RowDefinitions="30,25,*,*">
      <TextBlock Grid.Row="0"
                 Text="LedArray"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Bottom"
                 FontWeight="Bold"
                 FontSize="17" />
      <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="2">
        <TextBlock Text="{Binding AppVersion}"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   FontWeight="Bold"
                   FontSize="17"
                   Margin="5,0,0,0" />
        <Button Background="Transparent" Command="{Binding ShowAboutCommand}">
          <LucideIcon Kind="Info" Size="16" Foreground="{Binding IconColor}" />
        </Button>
      </StackPanel>
        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="Hardware &amp; Software Platform" HorizontalAlignment="Left"
                       Foreground="{StaticResource BlueAccentColor}" MaxWidth="240" VerticalAlignment="Center" FontWeight="Bold" TextWrapping="WrapWithOverflow" FontSize="12" />
        <ThemeVariantScope Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Grid.ColumnSpan="2"
                                 RequestedThemeVariant="Light">
          <Image VerticalAlignment="Center"
                 HorizontalAlignment="Right"
                 Margin="0,5,5,0"
                 IsVisible="{Binding !IsDarkMode}"
                 Source="/Assets/cf-logo.png" />
        </ThemeVariantScope>
        <ThemeVariantScope Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Grid.ColumnSpan="2"
                           RequestedThemeVariant="Dark">
          <Image VerticalAlignment="Center"
                 HorizontalAlignment="Right"
                 Margin="0,5,5,0"
                 IsVisible="{Binding IsDarkMode}"
                 Source="/Assets/cf-logo-white-lettering.png" />
        </ThemeVariantScope>
        <ToggleSwitch Grid.Row="0" Grid.Column="4" Grid.RowSpan="2" Margin="10,0,0,0" ToolTip.Tip="Change theme"
                            Content="Theme" FontWeight="Normal" FontSize="14"
                            IsChecked="{Binding IsDarkMode}">
          <ToggleSwitch.OnContent>
            <LucideIcon Kind="Moon" Size="18" Foreground="{Binding IconColor}" />
          </ToggleSwitch.OnContent>
          <ToggleSwitch.OffContent>
            <LucideIcon Kind="Sun" Size="18" Foreground="{Binding IconColor}" />
          </ToggleSwitch.OffContent>
        </ToggleSwitch>
    </Grid>

    <Grid DockPanel.Dock="Bottom" RowDefinitions="Auto" ColumnDefinitions="Auto,*,Auto,Auto" Margin="10">
      <Button Grid.Row="0" Grid.Column="3"
              Command="{Binding SaveConfigurationCommand}"
              Content="Save configuration"
              Margin="10,0,0,0"
              Background="{StaticResource BlueAccentColor}"
              Foreground="White">
        <Button.CommandParameter>
          <system:Boolean>false</system:Boolean>
        </Button.CommandParameter>
      </Button>
      <Button Grid.Row="0" Grid.Column="2" Command="{Binding SaveConfigurationCommand}"
              Content="Save configuration permanently">
        <Button.CommandParameter>
          <system:Boolean>true</system:Boolean>
        </Button.CommandParameter>
      </Button>
      <Button Grid.Row="0" Grid.Column="0" Command="{Binding ResetConfigurationCommand}"
              Content="Reset to defaults" />
    </Grid>

    <ScrollViewer HorizontalScrollBarVisibility="Auto" Padding="0">
      <StackPanel Spacing="12" Margin="0,20,0,0">
        <Panel Height="36">
          <TextBlock Text="Device connection" VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold"
                     Margin="10,0" />
        </Panel>
        <Grid ColumnDefinitions="*,*,*,*,*" RowDefinitions="*,*" Margin="0,0,50,0" MinHeight="60">
          <StackPanel Grid.RowSpan="2" VerticalAlignment="Bottom" Margin="10" Orientation="Horizontal"
                      Spacing="20">
            <TextBlock Text="Select a device" VerticalAlignment="Center" />
            <ComboBox Name="Ports" ItemsSource="{Binding Ports}" SelectedItem="{Binding SelectedPort}"
                      MinWidth="70" VerticalAlignment="Center" />
            <Button Command="{Binding LoadDeviceInformation}">
              <LucideIcon Kind="RefreshCw" Size="18" Foreground="{Binding IconColor}" />
            </Button>
            <Button Content="{Binding ConnectButtonText}"
                    Command="{Binding ConnectAndGetBaseInfoCommand}" />
            <ToggleButton Grid.Row="0" Grid.Column="2"
                    Command="{Binding ShowMessagesCommand}">
              <LucideIcon Kind="FileText" Size="18" Foreground="{Binding IconColor}" />
            </ToggleButton>
          </StackPanel>
          <Grid Grid.Row="0" Grid.Column="1" ColumnDefinitions="25,25,30,*" RowDefinitions="*,*,*"
                Margin="20, 0, 0, 0" IsVisible="{Binding Connected}">
            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" VerticalAlignment="Center"
                       HorizontalAlignment="Left" Text="{Binding DeviceName}" FontWeight="Bold"
                       FontSize="24" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Hw " VerticalAlignment="Bottom" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HardwareVersion}" FontWeight="Bold"
                       VerticalAlignment="Bottom" />
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Fw " VerticalAlignment="Top" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding FirmwareVersion}" FontWeight="Bold"
                       VerticalAlignment="Top" />
            <TextBlock Grid.Row="1" Grid.Column="2" Grid.RowSpan="2" Text="S/N " VerticalAlignment="Center" />
            <TextBlock Grid.Row="1" Grid.Column="3" Grid.RowSpan="2" Text="{Binding SerialNumber}"
                       FontWeight="Bold" VerticalAlignment="Center" />
          </Grid>
        </Grid>
        <StackPanel Orientation="Horizontal">
          <TabControl SelectedIndex="1" Margin="0,0,10,0">
            <!-- Control and Events Column -->
            <TabItem Header="Control and Events" FontSize="20" FontWeight="SemiBold">
              <ScrollViewer>
                <StackPanel Spacing="20" Margin="0,10,0,0">
                  <!-- Enable Events -->
                  <controls:RegisterField Header="Enable Device Events"
                                          Description="Specifies all the active events in the device.">
                    <StackPanel Spacing="10">
                      <CheckBox IsChecked="{Binding IsEnableLedEnabled}"
                                IsEnabled="True">
                        EnableLed
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsDigitalInputStateEnabled}"
                                IsEnabled="True">
                        DigitalInputState
                      </CheckBox>
                    </StackPanel>
                  </controls:RegisterField>

                  <!-- Register: DigitalInputState (Read-Only, Event) -->
                  <controls:RegisterField Header="DigitalInputState"
                                              Description="State of the digital input pins. An event will be emitted when the value of any digital input pin changes.">
                    <StackPanel Orientation="Vertical" Spacing="10">
                      <StackPanel Orientation="Horizontal" Spacing="5">
                        <controls:VisualStatus Status="{Binding IsDI0Enabled_DigitalInputState}"
                                               Width="15" Height="15" />
                        <TextBlock Text="DI0" VerticalAlignment="Center" />
                      </StackPanel>
                      <StackPanel Orientation="Horizontal" Spacing="5">
                        <controls:VisualStatus Status="{Binding IsDI1Enabled_DigitalInputState}"
                                               Width="15" Height="15" />
                        <TextBlock Text="DI1" VerticalAlignment="Center" />
                      </StackPanel>
                    </StackPanel>
                    </controls:RegisterField>
                </StackPanel>
              </ScrollViewer>
            </TabItem>

            <!-- LED0 Tab -->
            <TabItem Header="LED Array 0" FontSize="20" FontWeight="SemiBold">
              <ScrollViewer>
                <StackPanel Spacing="10"
                            Background="{Binding SurfaceBackground}"
                            controls:GroupBox.SurfaceBackground="{Binding SurfaceBackground}">
                  <!-- Register: PulseMode (Write) -->
                  <controls:RegisterField Header="LED0 Pulse Mode"
                                              Description="Configuration of the pulse mode used by the LEDs.">
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                      <ComboBox Grid.Row="0" Grid.Column="1"
                                SelectedItem="{Binding Led0SelectedPulseMode}"
                                Width="200"
                                HorizontalAlignment="Left">
                        <ComboBox.Resources>
                          <conv:EnumDisplayConverter x:Key="EnumDisplayConverter">
                            <conv:EnumDisplayConverter.Mappings>
                              <system:String x:Key="Pwm">PWM Mode</system:String>
                              <system:String x:Key="PulseTime">Interval mode</system:String>
                            </conv:EnumDisplayConverter.Mappings>
                          </conv:EnumDisplayConverter>
                        </ComboBox.Resources>
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <x:Static Member="local:PulseModeConfig.Pwm" />
                        <x:Static Member="local:PulseModeConfig.PulseTime" />
                      </ComboBox>
                    </Grid>
                  </controls:RegisterField>

                  <Grid RowDefinitions="*,*,*"
                        ColumnDefinitions="*,*,*"
                        IsVisible="{Binding IsLed0PwmMode}">
                    <!-- Register: Led0PwmFrequency (Write) -->
                    <controls:RegisterField Grid.Row="0"
                                            Grid.Column="0"
                                            Header="Frequency (Hz)"
                                            Description="Sets the frequency (Hz) of LED0 when in Pwm mode, between 0.5 and 2000.">
                      <NumericUpDown Value="{Binding Led0PwmFrequency}"
                                     Minimum="0.5"
                                     Maximum="2000"
                                     ClipValueToMinMax="True"
                                     Increment="0.1"
                                     FormatString="F2"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led0PwmReal (Read-Only) -->
                    <controls:RegisterField Grid.Row="0"
                                            Grid.Column="1"
                                            Header="Real Frequency (Hz)"
                                            Description="Get the real frequency (Hz) of LED0 when in Pwm mode.">
                      <TextBlock Text="{Binding Led0PwmReal}"
                                 HorizontalAlignment="Left"
                                 Margin="0,5,0,0"
                                 FontSize="14" />
                    </controls:RegisterField>

                    <!-- Register: Led0PwmDutyCycle (Write) -->
                    <controls:RegisterField Grid.Row="1"
                                            Grid.Column="0"
                                            Header="Duty Cycle (%)"
                                            Description="Sets the duty cycle (%) of LED0 when in Pwm mode, between 0.1 and 99.9.">
                      <NumericUpDown Value="{Binding Led0PwmDutyCycle}"
                                     Minimum="0.1"
                                     Maximum="99.9"
                                     ClipValueToMinMax="True"
                                     Increment="0.1"
                                     FormatString="F2"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led0PwmDutyCycleReal (Read-Only) -->
                    <controls:RegisterField Grid.Row="1"
                                            Grid.Column="1"
                                            Header="Real Duty Cycle (%)"
                                            Description="Get the real duty cycle (%) of LED0 when in Pwm mode.">
                      <TextBlock Text="{Binding Led0PwmDutyCycleReal}"
                                 HorizontalAlignment="Left"
                                 Margin="0,5,0,0"
                                 FontSize="14" />
                    </controls:RegisterField>

                    <!-- Register: Led0PwmPulseCounter (Write) -->
                    <controls:RegisterField Grid.Row="2"
                                            Grid.Column="0"
                                            Header="Number of pulses"
                                            Description="Sets the number of pulses of LED0 when in Pwm mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led0PwmPulseCounter}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>
                  </Grid>

                  <Grid RowDefinitions="*,*,*,*,*" ColumnDefinitions="*,*,*" IsVisible="{Binding IsLed0IntervalMode}">
                    <!-- Register: Led0PulseTimeOn (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="0"
                                            Header="Time ON (ms)"
                                            Description="Sets the time on (milliseconds) of LED0 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led0PulseTimeOn}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led0PulseTimeOff (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="1"
                                            Header="Time OFF (ms)"
                                            Description="Sets the time off (milliseconds) of LED0 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led0PulseTimeOff}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led0PulseTimePulseCounter (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="2"
                                            Header="Number of pulses"
                                            Description="Sets the number of pulses of LED0 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led0PulseTimePulseCounter}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led0PulseRepeatCounter (Write) -->
                    <controls:RegisterField Grid.Row="1" Grid.Column="0"
                                            Header="Number of repetitions"
                                            Description="Sets the number of repetitions of LED0 pulse protocol when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led0PulseRepeatCounter}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led0PulseTimeTail (Write) -->
                    <controls:RegisterField Grid.Row="1" Grid.Column="1"
                                            Header="Time between repetitions (ms)"
                                            Description="Sets the wait time between pulses (milliseconds) of LED0 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led0PulseTimeTail}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>
                  </Grid>

                  <!-- Register: AuxDigitalOutputState (Write) -->
                  <controls:RegisterField Header="AuxDigitalOutputState"
                                              Description="Write the state of the auxiliary digital output bits.">
                    <StackPanel Orientation="Vertical" Spacing="10">
                      <CheckBox IsChecked="{Binding IsAux0SetEnabled_AuxDigitalOutputState}"
                                IsEnabled="True">
                        Aux0Set
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsAux1SetEnabled_AuxDigitalOutputState}"
                                IsEnabled="True">
                        Aux1Set
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsAux0ClearEnabled_AuxDigitalOutputState}"
                                IsEnabled="True">
                        Aux0Clear
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsAux1ClearEnabled_AuxDigitalOutputState}"
                                IsEnabled="True">
                        Aux1Clear
                      </CheckBox>
                    </StackPanel>
                  </controls:RegisterField>
              
                  <!-- Register: DigitalInputTrigger (Write) -->
                  <controls:RegisterField Header="DigitalInputTrigger"
                                              Description="Configuration of the digital inputs pins behavior.">
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                      <TextBlock Grid.Row="0" Grid.Column="0"
                                 Text="DI0Trigger:"
                                 VerticalAlignment="Center"
                                 Margin="0,5,10,5" />
                      <ComboBox Grid.Row="0" Grid.Column="1"
                                SelectedItem="{Binding DigitalInputTrigger,
                                         Converter={StaticResource PayloadFieldConverter},
                                         ConverterParameter='DI0Trigger'}"
                                Width="200"
                                HorizontalAlignment="Left">
                        <x:Static Member="local:DigitalInputTriggerConfig.Led0EnablePower" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led0EnableBehavior" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led0EnableLed" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led1EnablePower" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led1EnableBehavior" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led1EnableLed" />
                        <x:Static Member="local:DigitalInputTriggerConfig.None" />
                      </ComboBox>
                      <TextBlock Grid.Row="1" Grid.Column="0"
                                 Text="DI1Trigger:"
                                 VerticalAlignment="Center"
                                 Margin="0,5,10,5" />
                      <ComboBox Grid.Row="1" Grid.Column="1"
                                SelectedItem="{Binding DigitalInputTrigger,
                                         Converter={StaticResource PayloadFieldConverter},
                                         ConverterParameter='DI1Trigger'}"
                                Width="200"
                                HorizontalAlignment="Left">
                        <x:Static Member="local:DigitalInputTriggerConfig.Led0EnablePower" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led0EnableBehavior" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led0EnableLed" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led1EnablePower" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led1EnableBehavior" />
                        <x:Static Member="local:DigitalInputTriggerConfig.Led1EnableLed" />
                        <x:Static Member="local:DigitalInputTriggerConfig.None" />
                      </ComboBox>
                    </Grid>
                  </controls:RegisterField>

                  <!-- Register: DigitalOutputState (Write) -->
                  <controls:RegisterField Header="DigitalOutputState"
                                              Description="Write the state of digital output lines.">
                    <StackPanel Orientation="Vertical" Spacing="10">
                      <CheckBox IsChecked="{Binding IsDO0SetEnabled_DigitalOutputState}"
                                IsEnabled="True">
                        DO0Set
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsDO1SetEnabled_DigitalOutputState}"
                                IsEnabled="True">
                        DO1Set
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsDO0ClearEnabled_DigitalOutputState}"
                                IsEnabled="True">
                        DO0Clear
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsDO1ClearEnabled_DigitalOutputState}"
                                IsEnabled="True">
                        DO1Clear
                      </CheckBox>
                    </StackPanel>
                  </controls:RegisterField>
              
                  <!-- Register: EnableLed (Write, Event) -->
                  <controls:RegisterField Header="EnableLed"
                                              Description="Enables/disables the LEDs.">
                    <StackPanel Orientation="Vertical" Spacing="10">
                      <CheckBox IsChecked="{Binding IsLed0OnEnabled_EnableLed}"
                                IsEnabled="True">
                        Led0On
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed1OnEnabled_EnableLed}"
                                IsEnabled="True">
                        Led1On
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed0OffEnabled_EnableLed}"
                                IsEnabled="True">
                        Led0Off
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed1OffEnabled_EnableLed}"
                                IsEnabled="True">
                        Led1Off
                      </CheckBox>
                    </StackPanel>
                  </controls:RegisterField>

              <!-- Register: EnableLedMode (Write) -->
                  <controls:RegisterField Header="EnableLedMode"
                                              Description="Start/stop the LEDs according to the pulse configuration.">
                    <StackPanel Orientation="Vertical" Spacing="10">
                      <CheckBox IsChecked="{Binding IsLed0OnEnabled_EnableLedMode}"
                                IsEnabled="True">
                        Led0On
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed1OnEnabled_EnableLedMode}"
                                IsEnabled="True">
                        Led1On
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed0OffEnabled_EnableLedMode}"
                                IsEnabled="True">
                        Led0Off
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed1OffEnabled_EnableLedMode}"
                                IsEnabled="True">
                        Led1Off
                      </CheckBox>
                    </StackPanel>
                  </controls:RegisterField>

                  <!-- Register: EnablePower (Write) -->
                  <controls:RegisterField Header="EnablePower"
                                              Description="Control the enable of both LEDs' power supply.">
                    <StackPanel Orientation="Vertical" Spacing="10">
                      <CheckBox IsChecked="{Binding IsLed0OnEnabled_EnablePower}"
                                IsEnabled="True">
                        Led0On
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed1OnEnabled_EnablePower}"
                                IsEnabled="True">
                        Led1On
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed0OffEnabled_EnablePower}"
                                IsEnabled="True">
                        Led0Off
                      </CheckBox>
                      <CheckBox IsChecked="{Binding IsLed1OffEnabled_EnablePower}"
                                IsEnabled="True">
                        Led1Off
                      </CheckBox>
                    </StackPanel>
                  </controls:RegisterField>

                </StackPanel>
              </ScrollViewer>
            </TabItem>

            <!-- LED1 Column -->
            <TabItem Header="LED Array 1" FontSize="20" FontWeight="SemiBold">
              <ScrollViewer>
                <StackPanel Spacing="10"
                            Background="{Binding SurfaceBackground}"
                            controls:GroupBox.SurfaceBackground="{Binding SurfaceBackground}">
                  <!-- Register: PulseMode (Write) -->
                  <controls:RegisterField Header="LED1 Pulse Mode"
                                              Description="Sets the pulse mode used by the LEDs.">
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                      <ComboBox Grid.Row="0" Grid.Column="1"
                                SelectedItem="{Binding Led1SelectedPulseMode}"
                                Width="200"
                                HorizontalAlignment="Left">
                        <ComboBox.Resources>
                          <conv:EnumDisplayConverter x:Key="EnumDisplayConverter">
                            <conv:EnumDisplayConverter.Mappings>
                              <system:String x:Key="Pwm">PWM Mode</system:String>
                              <system:String x:Key="PulseTime">Interval mode</system:String>
                            </conv:EnumDisplayConverter.Mappings>
                          </conv:EnumDisplayConverter>
                        </ComboBox.Resources>
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <x:Static Member="local:PulseModeConfig.Pwm" />
                        <x:Static Member="local:PulseModeConfig.PulseTime" />
                      </ComboBox>
                    </Grid>
                  </controls:RegisterField>

                  <Grid RowDefinitions="*,*,*" ColumnDefinitions="*,*,*" IsVisible="{Binding IsLed1PwmMode}">
                    <!-- Register: Led1PwmFrequency (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="0"
                                            Header="Frequency (Hz)"
                                            Description="Sets the frequency (Hz) of LED1 when in Pwm mode, between 0.5 and 2000.">
                      <NumericUpDown Value="{Binding Led1PwmFrequency}"
                                     Minimum="0.5"
                                     Maximum="2000"
                                     ClipValueToMinMax="True"
                                     Increment="0.1"
                                     FormatString="F2"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led1PwmReal (Read-Only) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="1"
                                            Header="Real Frequency (Hz)"
                                            Description="Get the real frequency (Hz) of LED1 when in Pwm mode.">
                      <TextBlock Text="{Binding Led1PwmReal}"
                                 HorizontalAlignment="Left"
                                 Margin="0,5,0,0"
                                 FontSize="14" />
                    </controls:RegisterField>

                    <!-- Register: Led1PwmDutyCycle (Write) -->
                    <controls:RegisterField Grid.Row="1" Grid.Column="0"
                                            Header="Duty Cycle (%)"
                                            Description="Sets the duty cycle (%) of LED1 when in Pwm mode, between 0.1 and 99.9.">
                      <NumericUpDown Value="{Binding Led1PwmDutyCycle}"
                                     Minimum="0.1"
                                     Maximum="99.9"
                                     ClipValueToMinMax="True"
                                     Increment="0.1"
                                     FormatString="F2"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led1PwmDutyCycleReal (Read-Only) -->
                    <controls:RegisterField Grid.Row="1" Grid.Column="1"
                                            Header="Real Duty Cycle (%)"
                                            Description="Get the real duty cycle (%) of LED1 when in Pwm mode.">
                      <TextBlock Text="{Binding Led1PwmDutyCycleReal}"
                                 HorizontalAlignment="Left"
                                 Margin="0,5,0,0"
                                 FontSize="14" />
                    </controls:RegisterField>

                    <!-- Register: Led1PwmPulseCounter (Write) -->
                    <controls:RegisterField Grid.Row="2" Grid.Column="0"
                                            Header="Number of pulses"
                                            Description="Sets the number of pulses of LED1 when in Pwm mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led1PwmPulseCounter}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>
                  </Grid>

                  <Grid RowDefinitions="*,*,*,*,*" ColumnDefinitions="*,*,*" IsVisible="{Binding IsLed1IntervalMode}">
                    <!-- Register: Led1PulseTimeOn (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="0"
                                            Header="Time ON (ms)"
                                            Description="Sets the time on (milliseconds) of LED1 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led1PulseTimeOn}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led1PulseTimeOff (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="1"
                                            Header="Time OFF (ms)"
                                            Description="Sets the time off (milliseconds) of LED1 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led1PulseTimeOff}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led1PulseTimePulseCounter (Write) -->
                    <controls:RegisterField Grid.Row="0" Grid.Column="2"
                                            Header="Number of pulses"
                                            Description="Sets the number of pulses of LED1 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led1PulseTimePulseCounter}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led1PulseRepeatCounter (Write) -->
                    <controls:RegisterField Grid.Row="1" Grid.Column="0"
                                            Header="Number of repetitions"
                                            Description="Sets the number of repetitions of LED1 pulse protocol when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led1PulseRepeatCounter}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>

                    <!-- Register: Led1PulseTimeTail (Write) -->
                    <controls:RegisterField Grid.Row="1" Grid.Column="1"
                                            Header="Time between repetitions (ms)"
                                            Description="Sets the wait time between pulses (milliseconds) of LED1 when in PulseTime mode, between 1 and 65535.">
                      <NumericUpDown Value="{Binding Led1PulseTimeTail}"
                                     Minimum="1"
                                     Maximum="65535"
                                     ClipValueToMinMax="True"
                                     Increment="1"
                                     FormatString="F0"
                                     Width="{StaticResource RegisterInputWidth}"
                                     HorizontalAlignment="Left" />
                    </controls:RegisterField>
                  </Grid>
                </StackPanel>
              </ScrollViewer>
            </TabItem>

            <!-- Digital & Power Column -->
            <TabItem Header="Digital &amp; Power" FontSize="20" FontWeight="SemiBold">
              <ScrollViewer>
                <StackPanel Spacing="10"
                            Background="{Binding SurfaceBackground}"
                            controls:GroupBox.SurfaceBackground="{Binding SurfaceBackground}">
                  <!-- Register: DigitalOutputSync (Write) -->
                  <controls:RegisterField Header="Digital Outputs Configuration"
                                              Description="Configuration of the synchronization of the digital outputs with the LEDs state.">
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                      <TextBlock Grid.Row="0" Grid.Column="0"
                                 Text="DO0 Config"
                                 VerticalAlignment="Center"
                                 Margin="0,5,10,5" />
                      <ComboBox Grid.Row="0" Grid.Column="1"
                                SelectedItem="{Binding DigitalOutputSync,
                                     Converter={StaticResource PayloadFieldConverter},
                                     ConverterParameter='DO0Sync'}"
                                Width="220"
                                HorizontalAlignment="Left"
                                Margin="0,5,0,5">
                        <ComboBox.Resources>
                          <conv:EnumDisplayConverter x:Key="EnumDisplayConverter">
                            <conv:EnumDisplayConverter.Mappings>
                              <system:String x:Key="None">Pure digital output (software controlled)</system:String>
                              <system:String x:Key="MimicLed0EnablePower">Mimics LED0 Enable Power</system:String>
                              <system:String x:Key="MimicLed0EnableBehavior">Mimics LED0 Enable Behavior</system:String>
                              <system:String x:Key="MimicLed0EnableLed">Mimics LED0 Enable LED</system:String>
                            </conv:EnumDisplayConverter.Mappings>
                          </conv:EnumDisplayConverter>
                        </ComboBox.Resources>
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <x:Static Member="local:DO0SyncConfig.None" />
                        <x:Static Member="local:DO0SyncConfig.MimicLed0EnablePower" />
                        <x:Static Member="local:DO0SyncConfig.MimicLed0EnableBehavior" />
                        <x:Static Member="local:DO0SyncConfig.MimicLed0EnableLed" />
                      </ComboBox>
                      <TextBlock Grid.Row="1" Grid.Column="0"
                                 Text="DO1 Config"
                                 VerticalAlignment="Center"
                                 Margin="0,5,10,5" />
                      <ComboBox Grid.Row="1" Grid.Column="1"
                                SelectedItem="{Binding DigitalOutputSync,
                                     Converter={StaticResource PayloadFieldConverter},
                                     ConverterParameter='DO1Sync'}"
                                Width="220"
                                HorizontalAlignment="Left"
                                Margin="0,5,0,5">
                        <ComboBox.Resources>
                          <conv:EnumDisplayConverter x:Key="EnumDisplayConverter">
                            <conv:EnumDisplayConverter.Mappings>
                              <system:String x:Key="None">Pure digital output (software controlled)</system:String>
                              <system:String x:Key="MimicLed1EnablePower">Mimics LED1 Enable Power</system:String>
                              <system:String x:Key="MimicLed1EnableBehavior">Mimics LED1 Enable Behavior</system:String>
                              <system:String x:Key="MimicLed1EnableLed">Mimics LED1 Enable LED</system:String>
                            </conv:EnumDisplayConverter.Mappings>
                          </conv:EnumDisplayConverter>
                        </ComboBox.Resources>
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <x:Static Member="local:DO1SyncConfig.None" />
                        <x:Static Member="local:DO1SyncConfig.MimicLed1EnablePower" />
                        <x:Static Member="local:DO1SyncConfig.MimicLed1EnableBehavior" />
                        <x:Static Member="local:DO1SyncConfig.MimicLed1EnableLed" />
                      </ComboBox>
                    </Grid>
                  </controls:RegisterField>

                  <!-- Register: Led0Power (Write) -->
                  <controls:RegisterField Header="LED Power Configuration"
                                              Description="Configuration of the power applied to the LEDs.">
                    <NumericUpDown Value="{Binding Led0Power}"
                                   Minimum="1"
                                   Maximum="120"
                                   ClipValueToMinMax="True"
                                   Increment="1"
                                   FormatString="F0"
                                   Width="200"
                                   HorizontalAlignment="Left"
                                   Margin="0,5,0,0" />
                    </controls:RegisterField>

                  <!-- Register: Led1Power (Write) -->
                  <controls:RegisterField Header="LED1 Power"
                                              Description="Sets the power to LED1, between 1 and 120 (arbitrary units).">
                    <NumericUpDown Value="{Binding Led1Power}"
                                   Minimum="1"
                                   Maximum="120"
                                   ClipValueToMinMax="True"
                                   Increment="1"
                                   FormatString="F0"
                                   Width="200"
                                   HorizontalAlignment="Left"
                                   Margin="0,5,0,0" />
                  </controls:RegisterField>

                  <!-- Register: AuxLedPower (Write) -->
                  <controls:RegisterField Header="Auxiliary LED Power"
                                              Description="Configuration of the power applied to the auxiliary LED.">
                    <NumericUpDown Value="{Binding AuxLedPower}"
                                   Minimum="1"
                                   Maximum="120"
                                   ClipValueToMinMax="True"
                                   Increment="1"
                                   FormatString="F0"
                                   Width="200"
                                   HorizontalAlignment="Left"
                                   Margin="0,5,0,0" />
                    </controls:RegisterField>
                </StackPanel>
              </ScrollViewer>
            </TabItem>
          </TabControl>

          <!-- Message Control -->
          <StackPanel>
            <controls:WriteMessagesControl MinWidth="500" IsVisible="{Binding ShowWriteMessages}" />
          </StackPanel>
        </StackPanel>
       </StackPanel>
     </ScrollViewer>
  </DockPanel>
</UserControl>
